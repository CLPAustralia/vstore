{% extends 'base.html.twig' %}

{% block title%}
  {{ 'product.new'|trans }}
{% endblock%}

{% block body %}
  <h3>{{ 'product.new'|trans }}</h3>
  {# TEST ONLY for JQuery UIi (class attribute) #}
  {{ form_start(form, {'attr':{'class':'ui-widget'}}) }}
    {{ form_widget(form) }}
  {{ form_end(form) }}

{% endblock %}

